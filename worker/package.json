{
  "name": "step-tracker-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for fetching Garmin step data and uploading to R2",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "garmin-connect": "^1.6.0"
  },
  "devDependencies": {
    "wrangler": "^3.85.0"
  }
}
